FROM python:3.11-slim

WORKDIR /app

COPY auth-requirements.txt .
RUN pip install --no-cache-dir -r auth-requirements.txt

COPY auth-service.py .

EXPOSE 5000

CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "2", "--timeout", "30", "auth-service:app"]
